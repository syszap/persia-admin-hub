# PostgreSQL (used by postgres service)
POSTGRES_DB=persia_admin
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# PostgreSQL (used by app service)
PG_HOST=postgres
PG_PORT=5432
PG_DATABASE=persia_admin
PG_USER=postgres
PG_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Application
PORT=58000
JWT_SECRET=CHANGE_ME_SUPER_SECRET
FRONTEND_URL=

# SQL Server sync (optional - leave empty if not used)
SQL_SERVER_CONN=
